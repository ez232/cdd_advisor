<%= form_for(@audio_device) do |f| %>
  <% if @audio_device.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@audio_device.errors.count, "error") %> prohibited this audio_device from being saved:</h2>

      <ul>
      <% @audio_device.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :proposal_id %><br />
    <%= f.number_field :proposal_id %>
  </div>
  <div class="field">
    <%= f.label :range_of_volume_specifications %><br />
    <%= f.text_field :range_of_volume_specifications %>
  </div>
  <div class="field">
    <%= f.label :sound_volume_specifications %><br />
    <%= f.text_field :sound_volume_specifications %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
